<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">#000000</item>
        <item name="android:colorPrimaryDark">@color/darkBrown</item>
        <item name="android:colorAccent">@color/darkBrown</item>
        <item name="android:colorPrimary">@color/darkBrown</item>
        <item name="android:navigationBarColor">@color/darkBrown</item>
        <item name="android:editTextBackground">@android:color/transparent</item>
    </style>

</resources>
